Updated Plan
└ □ Capture UX/content references (full screenshot + parallax slices) and
define structure/assets storage strategy.
□ Translate provided imagery/text into JSON data model; wire Svelte
components (hero, sections, parallax scenes, sidebar) that render from
fetched data.
□ Implement parallax behaviour (scene layers, scroll handling), lazy/
responsive images, sticky nav; integrate assets and ensure layout
matches reference via iteration.
□ Verify build/test (bun dev instructions, run lint/build if feasible) and
summarize verification steps/issues.
