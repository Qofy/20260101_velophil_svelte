• Updated Plan
└ ✔ Audit existing frontend template (deps, Routify setup, assets) and gather provided references (layout snapshot, parallax slices).
□ Model structured content in JSON (text, metadata) and implement Routify + Svelte 3.59 frontend with reusable sections, parallax scenes, lazy images, responsive
layout.
□ Integrate assets (parallax scenes, other images placeholders), ensure WASM integration unaffected, implement fetch + stores pipeline.
□ Implement styling, parallax scroll logic, lazy loading, nav behavior; test build to ensure success.
□ Verify UI/components vs reference images where possible, document verification + follow-up steps.
