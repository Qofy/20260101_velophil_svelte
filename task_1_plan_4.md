• Updated Plan
└ ✔ Define JSON schema + content extraction for article sections, parallax
    scenes, sidebar; map routes/components to use existing Svelte template.
  ✔ Implement Svelte components (nav, hero, parallax scene, sections,
    sidebar) and data fetch logic.
  □ Apply styling, parallax logic, responsive images; integrate assets and
    verify build/tests.
